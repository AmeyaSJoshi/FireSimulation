# Approach Registry

Append one record for every implementation or calibration approach. Keep rejected approaches so later runs do not rediscover them.

| ID | Approach | Scope | Status | Evidence | Decision |
|---|---|---|---|---|---|
| RUN-000 | Existing LANDFIRE WCS adapter with FCCS/WorldCover fallback | `landfireFuel.js`, Vite route, UI | baseline audit | pending | Keep as baseline until coverage/alignment contract is hardened |
| RUN-001 | Product-specific bounds, 95% valid-cell gate, GeoTIFF CRS/bounds/resolution/north-up validation | `landfireFuel.js`, `vite.config.js`, `src/main.js`, focused tests | accepted | 337 tests pass; live WCS still externally blocked | Promote only validated regional fields; partial/misaligned fields fail closed |
| RUN-002 | Offline asymmetric GeoTIFF contract fixture through decode, geometry validation, FBFM40 mapping, and aligned field construction | `scripts/validate-regional-inputs.mjs`, `package.json` | accepted | `npm run validate:regional-inputs` pass; `accuracyClaim:false` | Contract evidence only; not a fire-spread or live-source accuracy result |
| RUN-003 | Historical weather post-window blocking with explicit validity metadata and finite-window persistence tests | `firePropagation.js`, `firePropagation.test.js` | accepted | 34 propagation tests pass; post-window assertion passes | Multi-entry historical timelines stop producing new spread after the final observation; one-entry snapshots remain usable |
| RUN-004 | Offline end-to-end regional fixture plus one-command reproducible suite | `scripts/validate-regional-inputs.mjs`, `scripts/validate-regional-offline.mjs`, `package.json` | accepted | `npm run validate:regional-offline` pass; propagation reaches asymmetric neighbor at 1565.64 minutes; `accuracyClaim:false` | Demonstrates parser-to-field-to-propagation plumbing without pretending a synthetic raster is live or accurate |
| RUN-005 | Frozen public ArcGIS perimeter expansion to six cases with source hashes and case-specific grids | `officialBenchmarkFixtures.js`, `hackathonBenchmarkRunner.js`, `benchmark-manifest.json` | accepted as diagnostic data freeze | Six cases: 4 calibration, 2 holdout; default mean IoU 0.3084646 and mean F1 0.4406033; `accuracyClaim:false` | Adds reproducible perimeter diversity and prevents a two-case evaluator from being mistaken for regional validation; fuel, weather, terrain, ignition, suppression, and spotting remain synthetic or missing |
| RUN-006 | Four-case calibration sweep with untouched two-case holdout check | `scripts/calibrate-hackathon-benchmarks.mjs` | accepted as diagnostic only | 48 trials; best calibration mean IoU 0.2646924; holdout mean IoU 0.3758547; no defaults changed | Shows split discipline and exposes cross-fire weakness; it is not model training or operational calibration |
| RUN-007 | Bundle one real, successfully-fetched LANDFIRE FBFM40 GeoTIFF sample with full provenance and decode it through the real parser | `public/fixtures/landfire/`, `src/lib/landfireFuel.test.js` | superseded by RUN-008 | `node --test src/lib/landfireFuel.test.js` 7/7 pass; live WCS succeeded 1 of 4 attempts (HTTP 200), matching prior instability the other 3 tries | Confirmed decode/CRS/orientation/fuel-code mapping work on real data; discovered `validateLandfireFuelRasterGeometry` rejects this real response's bbox. Initially locked in as a documented, passing throw rather than blindly loosened; root-caused and fixed in RUN-008 |
| RUN-008 | Root-cause fix for the RUN-007 geometry gate failure: bbox containment + magnitude-based resolution check, replacing strict-equality + signed-resolution check | `src/lib/landfireFuel.js`, `src/lib/landfireFuel.test.js`, `scripts/validate-regional-inputs.mjs` | accepted | 342/342 tests pass; 4 independently-fetched real live samples (different CONUS locations/sizes) all pass; `validate:regional-inputs`/`validate:regional-offline` pass; browser smoke test on a CONUS click ran cleanly through fallback with no console errors when live WCS 502'd mid-session | Two real causes found via systematic debugging (not tuned to pass): (1) live GeoServer WCS pads/resamples the output extent to its native grid, up to 1.6x coarser resolution, confirmed across 4 samples; (2) geotiff.js reports opposite resolution-y sign for ModelTransformation- vs ModelPixelScale-encoded GeoTIFFs, so the old sign check only ever worked on the synthetic fixture. Fix is containment- and magnitude-bounded (4x cap, ~2.5x margin over observed worst case), not an unbounded loosening |
| RUN-009 | Attempt to capture a live LANDFIRE success screenshot for RUN-008's fix | none (investigation only) | blocked, external | direct curl confirmed the flakiness is real and external within a 5-minute window (3/3 succeeded then 3/3 failed with SSL_ERROR_SYSCALL, no app code involved) | Not a code defect; nothing further to do locally, retry when convenient |
| RUN-010 | Wire real per-field terrain/fuel/weather into the 6-case hackathon benchmark via pure reuse of existing fetchers (`fetchElevationField`, `fetchHistoricalWeatherInputs`, `readWorldCoverFineField`), frozen offline by a new one-time script | `scripts/build-regional-benchmark-fields.mjs` (new), `src/lib/hackathonBenchmarkRunner.js`, `src/lib/hackathonBenchmarkRunner.test.js`, `src/lib/regionalBenchmarkFields.generated.json` (new, committed fixture) | superseded by RUN-011 for terrain | 343/343 tests pass; `validate:hackathon`/`validate:regional-offline` pass, offline validator confirmed network-independent; fuel=real and weather=real for all 6 cases, terrain=synthetic-flat for all 6 (Open-Meteo elevation hit a documented daily quota, not a code bug) | Mean IoU dropped 0.308 -> 0.050 once real, heterogeneous fuel and real historical weather replaced the artificially generous homogeneous TU2/calm baseline; precision stayed 1.0, recall collapsed. Each field (terrain/fuel/weather) reports its own real-vs-fallback state per case rather than an all-or-nothing flag, so one exhausted upstream quota does not hide the two fields that did succeed |
| RUN-011 | Swap elevation source (Open-Meteo -> Copernicus DEM GLO-30 COG, unauthenticated AWS S3, no quota) and fix a real surfaceSpread.js physics bug that real terrain exposed | `scripts/build-regional-benchmark-fields.mjs`, `src/lib/surfaceSpread.js` | accepted as diagnostic only | 343/343 tests pass; `validate:rothermel`/`validate:rothermel:rate`/`validate:waf`/`validate:convergence` all still pass; `validate:hackathon`/`validate:regional-offline` pass; 6/6 cases now terrain=real, fuel=real, weather=real | Open-Meteo's elevation API is itself a proxy for Copernicus GLO-90 (coarser, quota-limited) -- reading Copernicus GLO-30 COG tiles directly reuses the exact windowed-read pattern already built for WorldCover fuel, no quota, finer resolution. That real terrain then exposed a genuine, previously-latent bug: the wind+slope combined-forcing-direction heuristic in `calculateSurfaceSpread` could label the wrong direction as "head" (sub-linear wind response vs quadratic slope response), violating the ellipse transform's head>=backing invariant by ~2.3% -- impossible to trigger with flat terrain (slopeRadians always exactly 0). Fixed at the computation source, not by loosening the fireEllipse.js assertion. Mean IoU with all 3 fields real: 0.0942 (vs 0.050 with fuel+weather-only, vs 0.308 fully synthetic); precision ~0.99, recall ~0.094 -- consistent underprediction against real conditions, not tuned |
